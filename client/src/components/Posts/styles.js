import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  mainContainer: {
    display: 'flex',
    alignItems: 'center',
  },
  lgMargin: {
    margin: theme.spacing(5),
  },
  actionDiv: {
    textAlign: 'center',
  },
}));